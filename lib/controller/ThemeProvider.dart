// import 'package:dynamic_theme/dynamic_theme.dart';
// import 'package:flutter/material.dart';

// class UserTheme {
//   ThemeData themeMode(brightness) =>
//       brightness == Brightness.light ? _lightMode : _darkMode;

//   void toggleBrightness(context) => DynamicTheme.of(context).setBrightness(
//         DynamicTheme.of(context).brightness == Brightness.light
//             ? Brightness.dark
//             : Brightness.light,
//       );

//   ThemeData get _lightMode => ThemeData.light();
//   ThemeData get _darkMode => ThemeData.dark();
// }
